{
  "name": "online-gmb-qr",
  "version": "1.0.0",
  "description": "GMB QR Code Design Generator",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=@canvas/client\" \"npm run dev --workspace=@canvas/server\"",
    "build": "npm run build --workspace=@canvas/client",
    "start": "npm start --workspace=@canvas/server",
    "install:all": "npm install && npm install --workspace=@canvas/client && npm install --workspace=@canvas/server"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}